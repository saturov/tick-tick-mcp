"""Embedded TickTick CLI helpers (vendored from ticktick-skill).

These modules are a one-time copy of the four `ticktick_cli` modules the
MCP server actually imports. Kept inside this package so the server is
self-contained — no sibling repo on sys.path.
"""